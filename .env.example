# Database (local Docker or Neon production)
DATABASE_URL="postgresql://talentflow:talentflow_dev@localhost:5432/talentflow_dev?schema=public"

# Server
PORT=4000
NODE_ENV=development

# CORS (comma-separated origins, defaults to localhost:4200)
CORS_ORIGINS=http://localhost:4200,http://localhost:4000
